---
title: "Popover"
description: "Documentation for the Popover component."
---

The `Popover` component represents a fundamental UI building block within the DesignForge token system.

## Installation

```bash
npm install @designforge/ui
```

## Usage

```tsx
import { Popover } from "@designforge/ui";

export default function App() {
  return (
    <Popover>Example Content</Popover>
  );
}
```

### Attributes

| Prop      | Type      | Default       | Description                                                 |
| :-------- | :-------- | :------------ | :---------------------------------------------------------- |
| `className` | `string`  | `undefined`   | Merges all optional utility styles directly. |


## Live View

Here is a live contextual rendering of the component directly from our isolated Storybook environment.

<StorybookPreview src="http://localhost:6006/iframe.html?id=overlay-popover--default&viewMode=story" />

> [!TIP]
> [Open Component in Storybook ↗](http://localhost:6006/?path=/story/overlay-popover--default)
