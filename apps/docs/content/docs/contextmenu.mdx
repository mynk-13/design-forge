---
title: "ContextMenu"
description: "Documentation for the ContextMenu component."
---

The `ContextMenu` component represents a fundamental UI building block within the DesignForge token system.

## Installation

```bash
npm install @designforge/ui
```

## Usage

```tsx
import { ContextMenu } from "@designforge/ui";

export default function App() {
  return (
    <ContextMenu>Example Content</ContextMenu>
  );
}
```

### Attributes

| Prop      | Type      | Default       | Description                                                 |
| :-------- | :-------- | :------------ | :---------------------------------------------------------- |
| `className` | `string`  | `undefined`   | Merges all optional utility styles directly. |


## Live View

Here is a live contextual rendering of the component directly from our isolated Storybook environment.

<StorybookPreview src="http://localhost:6006/iframe.html?id=overlay-contextmenu--default&viewMode=story" />

> [!TIP]
> [Open Component in Storybook ↗](http://localhost:6006/?path=/story/overlay-contextmenu--default)
