---
title: "DropdownMenu"
description: "Documentation for the DropdownMenu component."
---

The `DropdownMenu` component represents a fundamental UI building block within the DesignForge token system.

## Installation

```bash
npm install @designforge/ui
```

## Usage

```tsx
import { DropdownMenu } from "@designforge/ui";

export default function App() {
  return (
    <DropdownMenu>Example Content</DropdownMenu>
  );
}
```

### Attributes

| Prop      | Type      | Default       | Description                                                 |
| :-------- | :-------- | :------------ | :---------------------------------------------------------- |
| `className` | `string`  | `undefined`   | Merges all optional utility styles directly. |


## Live View

Here is a live contextual rendering of the component directly from our isolated Storybook environment.

<StorybookPreview src="http://localhost:6006/iframe.html?id=overlay-dropdownmenu--default&viewMode=story" />

> [!TIP]
> [Open Component in Storybook ↗](http://localhost:6006/?path=/story/overlay-dropdownmenu--default)
